import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'io.ionic.starter',
    appName: 'drumidi-tutor',
    webDir: 'build',
    bundledWebRuntime: false,
};

export default config;
